import { I<%= name %>Props } from '@/entities/<%= name %>'
import { IOptionsProps } from '@/entities/Options'
import { EmptyQueryPropsFactory, IQueryProps } from '@/entities/Query'

export interface I<%= name %>State {
  byIds: {
    [id: string]: I<%= name %>Props
  }
  query: IQueryProps
  options: IOptionsProps
  loading: boolean
  dialog: boolean | null
}

export const state = (): I<%= name %>State => ({
  byIds: {},
  query: EmptyQueryPropsFactory(),
  options: {},
  loading: false,
  dialog: null
})

export default state
